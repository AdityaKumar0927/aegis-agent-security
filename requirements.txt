# Core runtime dependencies only. pyproject.toml is the source of truth; this
# file is a convenience for `pip install -r requirements.txt`.
#
#   pip install -e ".[dev]"                          # tests, lint, types, langgraph
#   pip install "aegis-agent-security[langgraph]"    # runtime + LangGraph integration
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3
